// [UPDATED] 2026-03-14 16:35 - Configuración estándar Next.js sin exportación estática para desarrollo local
/** @type {import('next').NextConfig} */
const nextConfig = {
  // Standard local dev config — no static export
};

export default nextConfig;
